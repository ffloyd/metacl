for (int <%= n_iterator %>=<%= from[0] %>; <%= n_iterator %> < <%= to[0] %>; <%= n_iterator %>++)
{
    for (int <%= m_iterator %>=<%= from[1] %>; <%= m_iterator %> < <%= to[1] %>; <%= m_iterator %>++)
    {
<%= code %>
    }
}
