for (int <%= m_iterator %> = 0; <%= m_iterator %> < <%= m %>; <%= m_iterator %>++)
{
    for (int <%= n_iterator %> = 0; <%= n_iterator %> < <%= n %>; <%= n_iterator %>++)
    {
        <%= matrix_name %>[<%= n_iterator %> + <%= n %>*<%= m_iterator %>] = <%= expression %>;
    }
}